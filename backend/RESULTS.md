# DWSIM Agentic AI — Evaluation Summary

> Auto-generated by `eval_summary.py` from the repo's logged artifacts. Numbers are reported with sample sizes and a clear separation of *completed* vs *verified-correct* and *mock* vs *live DWSIM*. Re-run after a live benchmark to refresh the headline pass-rate.

## 1. Interaction sessions (`eval_log.json`)

| Metric | Value | What it means |
|---|---|---|
| Sessions logged | 78 | total recorded agent turns |
| Completed without error | 100% (78/78) | the turn finished — **not** a correctness measure |
| Sessions invoking >=1 tool | 38% (30/78) | i.e. did real flowsheet/sim work (rest are Q&A) |
| Turn duration | median 62s / mean 146s | latency |
| Sessions with LLM-judge scores | 2 | independent quality check coverage |
| &nbsp;&nbsp;judge: overall | 2/5 (n=2) | small sample — indicative only |
| &nbsp;&nbsp;judge: physical_plausibility | 2.5/5 (n=2) | small sample — indicative only |
| &nbsp;&nbsp;judge: property_package_correctness | 3/5 (n=2) | small sample — indicative only |
| &nbsp;&nbsp;judge: completeness | 2/5 (n=2) | small sample — indicative only |
| &nbsp;&nbsp;judge: hallucination_absence | 1.5/5 (n=2) | small sample — indicative only |

## 2. Hydrogen (biogas-SMR) case study (`hydrogen_results.jsonl`)

| Metric | Value | Note |
|---|---|---|
| Total runs | 68 | |
| Live-DWSIM runs | 0 | **0 = no live-engine validation yet** |
| Mock-bridge runs | 68 | exercise the workflow, not DWSIM physics |
| Converged | 100% (68/68) | of all runs |
| H2 yield (mock) | 63.2 mol% mean, range 53.2-68.5 | mock numbers |

## 3. Formal benchmark suite

Pass-rate: **24% (6/25)** across 25 tasks (mode: **live**); **32%** over the 19 tasks
that executed. Six tasks never ran (LLM rate-limiting) and are **inconclusive,
not failed**.

> **Convergence — read this before quoting a "100%" figure.** The `100% (68/68)`
> in §2 is the **mock** hydrogen study and `100% (78/78)` in §1 is **session
> completion** (explicitly *not* a correctness measure). **Neither is the live
> 25-task benchmark's solver convergence** — do not conflate them. The
> per-task `convergence` field in `benchmark_results.json` was previously an
> unreliable default (`True if stream_results else None`, which read `True` even
> for tasks that never executed) and **cannot support a "100% solver convergence"
> claim**. The harness now records the real per-task flag
> (`bridge.check_convergence` → `all_converged`, `None` for not-run tasks); a
> trustworthy live-benchmark convergence figure therefore awaits a re-run with
> the corrected harness.

Generate/refresh with `python run_benchmark_live.py` against a live DWSIM + LLM key.

## 4. Component-level correctness (unit tests)

`pytest -q` from `backend/`: **564 passed, 1 skipped** at time of writing (mock bridge/agent; live-DWSIM tests auto-skip). This validates the software components, not end-to-end task success against real DWSIM.

## 5. Honest verdict

- **Built / integrated:** yes — a genuine multi-step agent + DWSIM bridge + design + optimization stack, component-tested.
- **Validated end-to-end against live DWSIM:** **not yet evidenced** — all case-study runs are mock; the benchmark pass-rate is unrecorded; LLM-judge coverage is tiny (n=2).
- **To close the gap:** one live `run-all` benchmark execution + re-run this script.
